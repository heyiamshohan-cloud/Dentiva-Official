<UserControl x:Class="Dentiva.App.Views.NotificationsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="26,22,26,26" MaxWidth="980" HorizontalAlignment="Left">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Notifications" Style="{StaticResource Text.PageTitle}" />
                <TextBlock Text="Reminders and alerts generated by Dentiva — nothing leaves this computer."
                           Style="{StaticResource Text.Secondary}" Margin="0,4,0,0" />
            </StackPanel>
            <Button Content="Mark all as read" Style="{StaticResource GhostButton}" HorizontalAlignment="Right"
                    VerticalAlignment="Center" Click="OnMarkAllRead" />
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="List">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource Card}" Padding="18,14" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <Border Width="36" Height="36" CornerRadius="18" VerticalAlignment="Top">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{StaticResource Brush.AccentSubtle}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Priority}" Value="Critical">
                                                    <Setter Property="Background" Value="{StaticResource Brush.DangerSubtle}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Priority}" Value="Important">
                                                    <Setter Property="Background" Value="{StaticResource Brush.WarningSubtle}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock x:Name="IconText" Text="&#xEA8F;" FontFamily="Segoe MDL2 Assets, Segoe Fluent Icons"
                                               FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                                               Foreground="{StaticResource Brush.AccentInk}" />
                                </Border>

                                <StackPanel Grid.Column="1" Margin="14,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Title}" FontFamily="{DynamicResource Font.Medium}" FontSize="{StaticResource FontSize.Body}" />
                                        <Border Background="{StaticResource Brush.SurfaceSunken}" CornerRadius="999" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding PriorityLabel}" FontSize="10" FontFamily="{DynamicResource Font.Medium}"
                                                       Foreground="{StaticResource Brush.InkTertiary}" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{Binding Message}" Style="{StaticResource Text.Small}" Margin="0,3,0,0" TextWrapping="Wrap" />
                                    <TextBlock Text="{Binding CreatedAt, Converter={StaticResource DateTimeConverter}}" Style="{StaticResource Text.Caption}" Margin="0,4,0,0" />
                                </StackPanel>

                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Top">
                                    <Button Content="Read" Style="{StaticResource GhostButton}" Padding="10,5" Margin="0,0,4,0"
                                            FontSize="11.5" Tag="{Binding Id}" Click="OnMarkRead" />
                                    <Button Content="Dismiss" Style="{StaticResource GhostButton}" Padding="10,5"
                                            FontSize="11.5" Tag="{Binding Id}" Click="OnDismiss" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
