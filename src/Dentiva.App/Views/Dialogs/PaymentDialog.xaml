<Window x:Class="Dentiva.App.Views.Dialogs.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Record Payment"
        Width="500" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize"
        Background="Transparent" AllowsTransparency="True">
    <Border Margin="16" Background="{StaticResource Brush.Surface}" CornerRadius="12"
            BorderBrush="{StaticResource Brush.BorderSubtle}" BorderThickness="1" Effect="{StaticResource Shadow.Dialog}">
        <StackPanel Margin="30,26">
            <TextBlock Text="Record Payment" Style="{StaticResource Text.Subtitle}" />
            <TextBlock x:Name="ContextLine" Style="{StaticResource Text.Secondary}" Margin="0,4,0,20" />

            <TextBlock Text="Amount *" Style="{StaticResource FormLabel}" Margin="0,0,0,6" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="10" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="AmountBox" Height="36" />
                <Button Grid.Column="2" x:Name="FullButton" Content="Full due" Style="{StaticResource GhostButton}"
                        Padding="12,7" Click="OnFullAmount" ToolTip="Fill the outstanding balance" />
            </Grid>

            <TextBlock Text="Method *" Style="{StaticResource FormLabel}" Margin="0,16,0,6" />
            <ComboBox x:Name="MethodBox" Height="36" SelectionChanged="OnMethodChanged" />

            <TextBlock x:Name="ReferenceLabel" Text="Reference" Style="{StaticResource FormLabel}" Margin="0,14,0,6" />
            <TextBox x:Name="ReferenceBox" Height="36" />

            <TextBlock Text="Payment date *" Style="{StaticResource FormLabel}" Margin="0,14,0,6" />
            <DatePicker x:Name="DateBox" Height="36" />

            <TextBlock Text="Notes" Style="{StaticResource FormLabel}" Margin="0,14,0,6" />
            <TextBox x:Name="NotesBox" Height="52" Style="{StaticResource MultilineTextBox}" MaxHeight="52" />

            <TextBlock x:Name="ErrorText" Foreground="{StaticResource Brush.Danger}" Style="{StaticResource Text.Small}"
                       Margin="0,12,0,0" Visibility="Collapsed" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Cancel" Width="104" Height="36" Margin="0,0,10,0" Click="OnCancel" />
                <Button x:Name="SaveButton" Content="Record Payment" Style="{StaticResource PrimaryButton}" Width="150" Height="36" Click="OnSave" IsDefault="True" />
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
